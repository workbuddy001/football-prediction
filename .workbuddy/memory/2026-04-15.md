# 2026-04-15 工作日志

## 情报加权赔付矩阵 V3 算法实现

### 问题诊断
- **根因**：V2面板的"原始赔付分"(2.58/3.90/3.29)是**硬编码假数据**
- 原因：正则从DOM刮取 `_payoutScore` 结果失败 → 回退到默认假值
- 数据链路断了：真值 `sc1/sc2/sc3`(line 800-802)从未传给情报面板

### V3改动（prematch.js）
1. **line 805**：新增 `window._payoutRawScores = {home:sc1, draw:sc2, away:sc3}` — 存储真实赔付分
2. **line 1578-1718**：整块重写 V2→V3
   - 数据源：从 `window._payoutRawScores` 读真值，不再用正则刮DOM
   - 公式改为非线性映射：
     - `gap = (客队分 - 主队分) / 100`
     - 主胜系数 = 1 - gap×0.4 [0.6~1.4]
     - 平局系数 = 1 - |gap|×0.15 [0.85~1.0]
     - 客胜系数 = 1 + gap×0.4 [0.6~1.4]
   - 新增交叉验证信号（情报倾向 vs 赔付最低方向）
   - 星级评定结合交叉验证状态

### 文件修改
- `static/js/prematch.js` — V3算法已就位，语法检查通过，**尚未提交到GitHub**
