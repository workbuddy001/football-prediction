# 2026-04-26 工作记录

## 命中率统计改为三列显示（续）

### 问题2：分母错误
用户点击"命中率"按钮后显示 `5场/5场(100%)`，但应该是 `5场/9场(55.6%)`。

原因：`s.hits / s.total` 的分母是"小3球场次"(5)，应该是"总样本数"(9)。

### 修复2
1. **前端** `togglePatternStats()`：修正 `data.total` → `stats.total`，`Math.round()` → `toFixed(1)`
2. **前端** `displayPatternStats()`：修正 `Math.round()` → `toFixed(1)`
3. 显示格式改为：`实际场次/总样本数(百分比%)`

### 提交
- commit `1e3f252`：修正命中率显示：分母改为总样本数
- 已推送到 GitHub main 分支

### 显示效果（修正后）
```
预判   | 样本 | 小3球(0-2)    | 恰好3球      | 大3球(4+)
小3球 | 9场  | 5场/9场(55.6%) | 3场/9场(33.3%) | 1场/9场(11.1%)
```
