# 2026-04-09

## football_web.py 重大升级：比分记录+复盘系统（之前会话）
- 新增数据层：`_scores.json`（比分记录）+ `_reviews/`（复盘日志文件夹）
- 新增API：POST `/api/score`（保存比分）、POST `/api/review`（生成复盘日志）、GET `/api/scores`、GET `/api/reviews`
- 复盘逻辑：对比预测vs实际，命中则总结有效信号，失败则反思原因

## 相似案例规则迭代（本次会话）
- **第1次迭代**（之前会话）：从松散匹配→严格4维匹配体系（赛事/球队/赔率/预测），门槛≥25分+2维度或≥35分或同队+同排除
- **第2次迭代**（之前会话）：修复前端JS嵌套模板字符串导致所有按钮失效的问题
- **第3次迭代**（本次）：**预测结果相同设为硬性门槛**——历史案例的预测方向必须与当前分析一致才入选，否则直接跳过。相似分门槛保持25/35不变
