# 2026-05-06 工作日志

## 画像规律标注（已生效 vs 仅参考）
- 问题：画像规律（profile_rules）中的`🚫排除X球`和`📈方向规律`只是展示文本，不参与三维排除和推荐决策
- 解决：在v36_analyzer.py第912行后新增标注逻辑，检查每个`🚫排除X球`规则对应的球数是否在三维排除的`excluded`列表中
- 输出格式：`profile_rules`从纯字符串改为`{'text': ..., 'active': True/False}`结构
- 前端更新：sporttery_web.py第2484行，`✅`绿色标记已生效，`[仅参考]`灰色标记未生效
- 验证案例：
  - 柏太阳神vs浦和红钻：`🚫排除7球` ACTIVE ✅（7球确实被排除），`📈高开+双方攻弱→小球70%` [仅参考]
  - 里昂vs雷恩：`🔥主让球→让胜75%` ACTIVE ✅，`🚫排除2球` [仅参考]（2球未被三维排除）

## hhad_lose_hint 修复
- 根因：`__pycache__`缓存导致旧代码执行，用`python -B`启动禁用pyc解决
- 548场中220场active，各tier（A-E）正常输出

## 旧让球盘规则清理 + 决策树集成
- 删除v36_analyzer.py第788-794行3条与新决策树矛盾的P0规则：
  1. `主让+让负<2.0→让负仅2%` (与S级83%矛盾)
  2. `让胜3.0-5.0→让负仅0%` (与让胜>3.0→让负53.8%矛盾)
  3. `让胜≥5→让负仅8%` (同上)
- v36_analyzer集成`_analyze_hhad_lose_low()`决策树，延迟导入避免循环依赖
- 决策树结果以`🔍决策树B级`标签写入画像规律，柏太阳神正确触发B级排除让负
- 已提交推送GitHub: 12ab39c → 6f8b48a

## 防守维度规律深度挖掘（525场回测）
- 用户直觉验证：塞维利亚vs皇家社会（主失1.6<客失1.8）→ 防守优势决定胜负
- 发现：失球差(def_diff)预测力跨度38pp > 攻击差23pp → 防守是更强的胜负信号
- 场景A验证：客攻强+主防强 → 主胜43% vs 客胜27%（102场），防守压过攻击
- 让球盘+防守：主让1球+主防劣→让负83%(最优子集，+14pp vs 基准69%)
- 大小球+防守核心发现：大球低水+主防劣→60%(+8pp) / 小球低水+主防优→48%(+5pp)
- 双方漏勺→大球77% / 双方铁壁→小球49%
- 新脚本：`_analyze_defense_outcome.py`(全面分析), `_analyze_defense_enhancement.py`(增强验证)

## 防守维度集成到V36 Step0 (V3.8)
- 在Step0方向判断中加入def_diff=主失-客失, def_dir(大球/小球/中性)
- 水位-防守一致性检查：一致强化(+8pp大球/+5pp小球) vs 矛盾(诱盘嫌疑)
- 防守信号计入all_dirs投票(权重1)
- 置信度修正：一致→升档, 矛盾→降档标注
- step0输出新增6个防守字段，前端新增防守一致性展示(✅水位一致/⚠️诱盘嫌疑)
- 已验证：里昂vs雷恩(一致强化✅), 柏太阳神vs浦和(诱盘嫌疑⚠️)
- Commit: 61ac20e → 已推送GitHub
