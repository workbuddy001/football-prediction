## 6月23日完整测试日志

### 上午：O1变化数据验证
- 变化数据（hhad_change）集成后准确率从40%降到33.3%
- 结论：**O1无效，放弃**

### 下午：比分反推 + 多方案优化测试

#### 各方案迭代
| 版本 | 描述 | 方向率 | 前3命中 | ROI（100场） |
|------|------|:------:|:-------:|:-----------:|
| V4基准 | 选最低赔 | 50% | - | 51.5%方向率 |
| V5初版 | 含比分反推 | 50% | - | 51%方向率 |
| V5+score_change | 排除升赔>10% | 40% | 40% | +0.0% |
| V5+近况加权 | 比分匹配近况优先 | 43% | 40% | +0.5% |
| V5+阈值20%+近况 | 缩小总进球范围 | 43% | 40% | +0.5% |
| **V5+加权分配** | **赔率权重投注** | 43% | 40% | **+15.5%** |

#### 最终策略验证（4-6月+1让球）
| 过滤方案 | 4月 | 5月 | 6月 | 合计ROI |
|---------|:---:|:---:|:---:|:-------:|
| 无过滤 | +3.8% | -3.7% | +14.5% | ~+2% |
| 赔率密集1.2过滤 | +0.8% | **+19.4%** | +11.7% | **+11.9%** |

**最终策略 = +1让球 + 阈值20% + 近况加权 + 权重分配 + 密集1.2过滤**
- 193场有效投注：投4996元回5589元，ROI **+11.9%** ✅

#### 晚：集成到ai_reasoning.py
- 在`compute_betting()`函数最前面（line 322后）插入**+1SCORE规则**
- 优先级最高：凡是+1让球+密度≥1.2，直接跳过R0/R1/S9/H9等所有规则
- 返回格式与原规则一致（rule=+1SCORE, score_bets含3个比分+权重）

#### 关键代码修改
- `estimate_goals()` 放宽范围
- `sort_key()` 加入近况匹配加权（deviation越小越优先）
- ROI计算改为赔率权重分配（`stake = (1/odds) / total_weight * 30`）
- `round_stake()` 新增：投注金额取2的倍数（符合竞彩规则，最少2元）
- `TG_MERGE_THRESHOLD` 从0.3改为0.2
- `DENSITY_THRESHOLD = 1.2` 新增赔率密集度过滤
- `ai_reasoning.py` 新增+1SCORE最高优先级规则

#### 新增文件
- `verify_handicap_method_v5.py` - 主脚本（含--density参数）
- `_june_backtest.py` / `_june_backtest_v2.py` / `_may_backtest.py` / `_apr_backtest.py` - 月度回测
- `_filter_test.py` - 过滤方案对比测试
- `june_results.json` / `may_results_v2.json` / `apr_results_v2.json` - 月度结果